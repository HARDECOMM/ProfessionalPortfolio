# Haruna Ademoye Portfolio

A simplified static portfolio showcasing MERN stack and Python expertise with a gallery, certifications, and CV download.

## 🚀 Run Locally
1. Clone this repository
2. Open `index.html` in your browser
3. Replace images inside `assets/` with your personal files
4. Add your CV as `assets/CV.pdf` for the download button

## 📂 Structure
- `assets/` → images, certificates, CV
- `scripts/main.js` → JavaScript logic
- `styles/styles.css` → styling
- `index.html` → main portfolio page
- `README.md` → instructions
